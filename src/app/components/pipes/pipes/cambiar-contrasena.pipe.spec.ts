import { CambiarContrasenaPipe } from './cambiar-contrasena.pipe';

describe('CambiarContrasenaPipe', () => {
  it('create an instance', () => {
    const pipe = new CambiarContrasenaPipe();
    expect(pipe).toBeTruthy();
  });
});
